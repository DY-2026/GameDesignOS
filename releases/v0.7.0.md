# v0.7.0 - GameDesignOS Public Identity

## Highlights

- Repositions the public project around `GameDesignOS by Paranoia`.
- Updates the canonical GitHub-facing links to `DY-2026/GameDesignOS`.
- Adds a new README hero banner with a deterministic title/signature layer.
- Consolidates the public proof paths into a clearer featured-cases section.
- Completes the seven-module visual showcase and makes each module card clickable.

### Added

- Added `assets/gamedesignos-overview-banner-v7-background.png` as the no-text banner background.
- Added `assets/gamedesignos-overview-banner-v7.png` as the README-facing `GameDesignOS by Paranoia` banner.
- Added the Dreamina-generated `assets/showcase-game-design-proposal-writer.png` so the README showcase now has visible art for all seven public skills.

### Changed

- Updated `README.md`, `README.en.md`, and `README.zh-CN.md` to show `v0.7.0`.
- Updated Star History, GitHub About metadata, showcase feedback links, and schema identifiers to `DY-2026/GameDesignOS`.
- Reworked the 60-second demo and featured-cases flow so the demo stays a quick interaction example, while `《生存33天》` and `《冒险家艾略特的千年奇谭》` live together in the case table.
- Updated the banner design notes and HyperFrames source template to reflect `GameDesignOS by Paranoia`.
- Reworked the README showcase into seven clickable module cards ordered by the GameDesignOS product path: concept, evidence, proposal, ED iteration, workflow governance, translation, and source curation.
- Updated current adapter docs, skill sub-README parent links, and the ED experiment schema id from the old `ParanoiaSkills` name to `GameDesignOS` / `GameDesignOS by Paranoia`.
- Marked the older public article as a historical `ParanoiaSkills` draft so it does not conflict with the v0.7.0 identity.

### Safety

- Kept the generated banner background free of critical text; the project name, version, and author signature are overlaid locally and deterministically.
- Kept public examples bounded by source status and public-material review language.

### Validation

- `python scripts/validate_repo.py`
