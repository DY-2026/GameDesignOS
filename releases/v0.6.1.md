# v0.6.1 - Repository Validation Fix

## Highlights

- Fixes the repository release validation health check by restoring `v0.6.0`-style required files and syncing public version info.

### Fixed

- Re-added `.gitignore` to tracked files to satisfy `scripts/validate_repo.py` required path checks.
- Updated the public `README` trio to reflect `v0.6.1`.
- Synced `CHANGELOG.md` compare link and version reference entries for the patch release.

### Changed

- Marked the patch change as formal unreleased-to-release housekeeping in version history.

### Validation

- `python scripts/validate_repo.py`
